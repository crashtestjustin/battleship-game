import { ship } from "./ship.js";

export const gameboard = () => {
  const initializeBoard = () => {
    let boardArray = [];
    let squareArray = [];
    for (let i = 0; i < 10; i++) {
      let alpha = ["A", "B", "C", "D", "E", "F", "G", "H", "I", "J"];
      for (let j = 0; j < 10; j++) {}
    }
  };
};
